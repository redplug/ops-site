services:
  ops-site:
    build:
      context: .
      dockerfile: Dockerfile
    image: ops-site:latest
    container_name: ops-site
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - ops-site-data:/app/data

volumes:
  ops-site-data:
